# CI 파이프라인 사실의 단일 소스. 서비스 추가·변경은 이 파일만 고치면 됨 — vars/ci.groovy 는 무수정.
defaults:
  scanGate: true
  sign: false

services:
  auth:
    scanGate: false # distroless 베이스 openssl CVE, fix 없음 — warn 유지
    sign: true
  core: {}
  batch: {}
